import React from 'react';
import Immutable from 'immutable';
import { shallow, mount } from 'enzyme';
import ModalMobileWarning from './ModalMobileWarning';

const minProps = {};

describe('<ModalMobileWarning />', () => {
	it('Should', () => {
		expect(true).toEqual(true);
	});
});
