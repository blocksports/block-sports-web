import React from 'react';
import Immutable from 'immutable';
import { shallow, mount } from 'enzyme';
import ModalDemoWarning from './ModalDemoWarning';

const minProps = {};

describe('<ModalDemoWarning />', () => {
	it('Should', () => {
		expect(true).toEqual(true);
	});
});
