import React from 'react';
import Immutable from 'immutable';
import { shallow, mount } from 'enzyme';
import MarketListSkeleton from './';

const minProps = {};

describe('<MarketListSkeleton />', () => {
	it('Should', () => {
		expect(true).toEqual(true);
	});
});
