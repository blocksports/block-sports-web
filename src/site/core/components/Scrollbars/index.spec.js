import React from 'react';
import Immutable from 'immutable';
import { shallow, mount } from 'enzyme';
import DefaultScrollbars from './';

const minProps = {};

describe('<DefaultScrollbars />', () => {
	it('Should', () => {
		expect(true).toEqual(true);
	});
});
