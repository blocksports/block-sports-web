import React from 'react';
import Immutable from 'immutable';
import { shallow, mount } from 'enzyme';
import NavListSkeleton from './';

const minProps = {};

describe('<NavListSkeleton />', () => {
	it('Should', () => {
		expect(true).toEqual(true);
	});
});
